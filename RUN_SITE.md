# Running the demo site

1. Install dependencies:

```bash
npm install
```

2. Start the server:

```bash
npm start
```

3. Open http://localhost:8080 in your browser.

This repository now includes a minimal Express server (`app.js`) that serves the static files in `/public`.
